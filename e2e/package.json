{
  "name": "e2e",
  "version": "1.0.0",
  "description": "",
  "main": "cypress.config.js",
  "scripts": {
    "start": "cd ../backend && npm start && cd ../e2e && wait-on http://localhost:3001 && cypress run", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.16.0"
  }
}
